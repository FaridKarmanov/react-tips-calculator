import { Color } from "../UI/colors";

export const CustomStyles = {
  control: () => ({
    width: "100%",
    display: "flex",
    alignItems: "center",
    background: Color.white,
    borderRadius: "30px",
    marginBottom: "45px",
    padding: "15px",
    border: "none ",
  }),

  indicatorSeparator: () => ({
    width: "0px",
  }),
};
